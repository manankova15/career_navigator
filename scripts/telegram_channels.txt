# Список Telegram-каналов с вакансиями для парсинга.
# Один юзернейм на строку (без @). Строки, начинающиеся с #, и пустые — игнорируются.
# Если файл пустой или отсутствует, используется переменная окружения TELEGRAM_CHANNEL (по умолчанию job_for_analysts).

job_for_analysts
vacancy_cs
jtbl_vacancy
